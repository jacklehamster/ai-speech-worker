{
  "name": "ai-speech-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts --config wrangler.dev.toml",
    "deploy": "wrangler deploy",
    "build": "bun i && tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "^4.12.0"
  },
  "dependencies": {
    "@dobuki/translation-sheet": "^1.0.5",
    "openai": "^4.95.1"
  }
}
